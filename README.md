# jw910731's Nixfile
Nix files that I manage my macbook and linux workstation environment.

# How to
You need to install nix by your own to proceed.

Leverage the `nix develop` comand to get a shell with everything you need.

Then,
```shell
just switch <target>
```
to activate corresponding profile

# Format
```shell
just fmt
```
